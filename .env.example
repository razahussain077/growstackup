# Where the free-sample form POSTs. Wire to Zapier / Make / your own API later.
# Public because it's a client-side fetch; use a webhook that only accepts new leads.
NEXT_PUBLIC_LEAD_WEBHOOK_URL=https://hooks.zapier.com/hooks/catch/000000/replace-me/
